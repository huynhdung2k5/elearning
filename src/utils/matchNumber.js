// ----------------------------------------------------------------------
// Lấy số ra từ chuỗi
// ----------------------------------------------------------------------

export function matchNumber(string) {
  return string?.match(/\d/g)?.join('');
}
